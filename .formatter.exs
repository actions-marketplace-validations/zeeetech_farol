# Used by "mix format"
[
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"],
  locals_without_parens: [
    assert_acessible: 2,
    assert_acessible: 1
  ],
  export: [
    locals_without_parens: [
      assert_acessible: 2,
      assert_acessible: 1
    ]
  ]
]
